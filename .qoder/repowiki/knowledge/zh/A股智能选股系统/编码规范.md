- 采用漏斗式筛选架构，每步筛选均记录输入输出数量并打印日志，便于追踪过滤效果。
- 数据获取模块统一采用“先查SQLite缓存，缺失则调用API并回写”的模式，支持增量更新以降低网络请求频率。
- 所有模块通过 `src/utils.setup_logging` 初始化独立的 Logger 实例，并同时输出到控制台和按模块命名的日志文件。
- 技术指标计算与信号判断逻辑解耦，分别实现在 `indicators` 子模块中，由 `filters` 子模块调用组合。